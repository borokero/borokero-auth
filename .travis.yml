sudo: required
dist: focal
language: node_js
cache:
  npm: false
addons:
  chrome: stable
node_js:
  - "14"

services:
  - docker

install:
  - npm install

before_install:
  - ./scripts/start-server.sh

before_script:
  - make lint

script: 
  - npm test